# =========================================================================
# Antigravity Global Governance .gitignore
# =========================================================================

# -------------------------------------------------------------------------
# 1. 状態管理・セッションデータ（絶対にGitから除外）
# -------------------------------------------------------------------------
# エージェントとの会話履歴や現在のコンテキスト状態はPC固有のプライベートデータです。
/context_state/
/conversations/
/implicit/
installation_id
mcp_config.json


# -------------------------------------------------------------------------
# 2. 自動生成物・アーティファクト（Gitから除外）
# -------------------------------------------------------------------------
# エージェントが実行結果として出力した静的ファイルや録画データは都度生成されるため除外します。
/browser_recordings/
/html_artifacts/

# -------------------------------------------------------------------------
# 3. 実験・テスト用ディレクトリ（Gitから除外）
# -------------------------------------------------------------------------
# 個人の砂場（サンドボックス）はリポジトリを汚染しないようにします。
/playground/

# -------------------------------------------------------------------------
# 4. データセット・巨大ファイル（条件付きで除外）
# -------------------------------------------------------------------------
# ベクトルDBのインデックスや巨大なモデルキャッシュがここに入る場合は除外します。
# ※もし /knowledge や /annotations がプレーンテキストの共有ルールブックであれば、
#   以下の2行はコメントアウトしてGit管理対象に含めてください。
/brain/
/knowledge/
/annotations/
__pycache__

# -------------------------------------------------------------------------
# 5. 非推奨ディレクトリ（移行を促すため除外）
# -------------------------------------------------------------------------
# 過去の会話で確認した通り、旧命名規則のディレクトリは管理対象外とし、
# /global_workflows/ への統合を強制します。
/workflows/

# =========================================================================
# 【ホワイトリスト】Git管理すべき主要ディレクトリ（参考）
# 以下のディレクトリは Git にコミットし、シンボリックリンクで共有します。
# - /skills/       : ロール定義や共通スキル
# - /workflows/    : プロセスやタスクの定義
# - /scripts/      : 環境チェックなどのユーティリティ
# - /governance/   : 全体ポリシーやルール
# - /prompting/    : プロンプトのテンプレート等
# =========================================================================
